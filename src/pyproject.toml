[tool.poetry]
name = "sandbox"
version = "0.1.0"
description = ""
authors = ["migmass <migmas@bk.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
